name: 'Preparation for the official start of the compilation steps.'

runs:
  using: 'composite'
  steps:

      # ============================================================
      # Get Kernel
      #
      # The main workflow now clones device_kernel BEFORE this
      # action runs. Therefore DO NOT clone it again here.
      # ============================================================

      - name: Get Kernel
        shell: bash
        run: |
          set -euxo pipefail

          kernel_dir="$GITHUB_WORKSPACE/device_kernel"

          echo "::group::[+] Validating kernel source..."

          if [[ ! -d "$kernel_dir" ]]; then
            echo "::error::Kernel source directory is missing: $kernel_dir"
            exit 1
          fi

          if [[ ! -f "$kernel_dir/Makefile" ]]; then
            echo "::error::Kernel source is invalid: $kernel_dir"
            echo "::error::Missing: $kernel_dir/Makefile"
            exit 1
          fi

          echo "[+] Kernel source found:"
          echo "    $kernel_dir"

          echo "::endgroup::"

          cd "$kernel_dir"

          KERNEL_VERSION=$(
            head -n 3 Makefile |
            grep -E 'VERSION|PATCHLEVEL' |
            awk '{print $3}' |
            paste -sd '.'
          )

          echo "[+] Kernel version: $KERNEL_VERSION"

          echo "KERNEL_VERSION=$KERNEL_VERSION" >> "$GITHUB_ENV"
          echo "FIRST_VERSION=$(echo "$KERNEL_VERSION" | awk -F '.' '{print $1}')" >> "$GITHUB_ENV"
          echo "SECOND_VERSION=$(echo "$KERNEL_VERSION" | awk -F '.' '{print $2}')" >> "$GITHUB_ENV"


      # ============================================================
      # Get Kernel Arch
      # ============================================================

      - name: Get Kernel Arch
        shell: bash
        run: |
          set -euxo pipefail

          cd "$GITHUB_WORKSPACE/device_kernel"

          if [[ -d "arch/arm64/configs" ]]; then
            echo "KERNEL_ARCH=arm64" >> "$GITHUB_ENV"
            echo "[+] Kernel architecture: arm64"

          else
            echo "[-] Non-ARM64 Devies not supported!"
            exit 1

          fi


      # ============================================================
      # Get Packing Tools
      # ============================================================

      - name: Get Packing Tools
        shell: bash
        run: |
          set -euxo pipefail

          # Set ${{ env.PACK_METHOD }}

          if [[ "${{ env.PACK_METHOD }}" == "Anykernel3" ]]; then
            cd "$GITHUB_WORKSPACE/device_kernel"

            if [[ -d "Anykernel3" ]]; then
              echo "[+] Found Anykernel3 in Kernel!"

            elif [[ "${{ env.AK3_SOURCE }}" == *"osm0sis/AnyKernel3.git" ]]; then
              git clone "${{ env.AK3_SOURCE }}" \
                -b "${{ env.AK3_BRANCH }}" \
                Anykernel3

              cd Anykernel3

              git checkout dca9dc370838d919d56c1f59ec78b27a14a72c68

              cd ../

              echo "[+] Get Anykernel3 successfully!"

            else
              git clone \
                "${{ env.AK3_SOURCE }}" \
                -b "${{ env.AK3_BRANCH }}" \
                Anykernel3 \
                --depth=1
            fi

          elif [[ "${{ env.PACK_METHOD }}" == "MKBOOTIMG" ]] || \
               [[ "${{ env.DEFCONFIG_FROM_BOOT }}" == "true" ]]; then

            cd "$GITHUB_WORKSPACE"

            git clone \
              https://android.googlesource.com/platform/system/tools/mkbootimg \
              "$GITHUB_WORKSPACE/mkboottools" \
              -b main-kernel-build-2024 \
              --depth=1

            ${{ env.CURLX }} \
              "${{ env.BOOT_SOURCE }}" \
              "$GITHUB_WORKSPACE/boot_source_${{ env.DEVICE_NAME }}.img"

            echo "[+] Get MKBOOTIMG successfully!"

            cd "$GITHUB_WORKSPACE/device_kernel"

          else
            echo "[-] Need packing method!"
            exit 1
          fi

          echo "PACK_METHOD=${{ env.PACK_METHOD }}" >> "$GITHUB_ENV"


      # ============================================================
      # Get DEFCONFIG
      # ============================================================

      - name: Get DEFCONFIG
        if: env.DEFCONFIG_SOURCE != '' || env.DEFCONFIG_FROM_BOOT == 'true'
        shell: bash
        run: |
          set -euxo pipefail

          cd "$GITHUB_WORKSPACE/device_kernel"

          if [[ -n "${{ env.DEFCONFIG_SOURCE }}" ]]; then

            ${{ env.CURLX }} \
              "${{ env.DEFCONFIG_SOURCE }}" \
              "device_kernel/arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

          elif [[ -z "${{ env.DEFCONFIG_ORIGIN_IMAGE }}" ]]; then

            if [[ "${{ env.DEFCONFIG_FROM_BOOT }}" == "true" ]]; then

              if [[ -f "${{ env.BOOT_SOURCE }}" ]]; then

                ${{ env.CURLX }} \
                  "${{ env.BOOT_SOURCE }}" \
                  "$GITHUB_WORKSPACE/boot.img"

                "$GITHUB_WORKSPACE/mkboottools/unpack_bootimg.py" \
                  --boot_img "$GITHUB_WORKSPACE/boot.img"

                mv \
                  "$GITHUB_WORKSPACE/out/kernel" \
                  "$GITHUB_WORKSPACE/device_kernel/Image"

                "$GITHUB_WORKSPACE/device_kernel/scripts/extract-ikconfig" \
                  "$GITHUB_WORKSPACE/device_kernel/Image" \
                  > "$GITHUB_WORKSPACE/device_kernel/arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

              fi

            else

              ${{ env.CURLX }} \
                "${{ env.DEFCONFIG_ORIGIN_IMAGE }}" \
                "$GITHUB_WORKSPACE/device_kernel/Image"

              "$GITHUB_WORKSPACE/device_kernel/scripts/extract-ikconfig" \
                "$GITHUB_WORKSPACE/device_kernel/Image" \
                > "$GITHUB_WORKSPACE/device_kernel/arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

            fi
          fi


      # ============================================================
      # Clean KernelSU and Hook
      # ============================================================

      - name: Cleaned KernelSU and Hook
        shell: bash
        run: |
          set -euxo pipefail

          cd "$GITHUB_WORKSPACE/device_kernel"

          echo "::group::[+] Starting clean hook..."

          cp /tmp/Bin/clean_hook.sh ./

          bash clean_hook.sh

          echo "::endgroup::"

          echo "[+] Cleaned hook."


      # ============================================================
      # Set KernelSU
      #
      # IMPORTANT:
      #
      # KernelSU-Next is prepared by the MAIN workflow at:
      #
      #   device_kernel/KernelSU
      #
      # We intentionally DO NOT create:
      #
      #   device_kernel/KernelSU-Next
      #
      # ============================================================

      - name: Set KernelSU
        shell: bash
        run: |
          set -euxo pipefail

          cd "$GITHUB_WORKSPACE/device_kernel"

          KERNELSU_AUTO(){

            if [[ "${{ env.KERNELSU_AUTO_FORK }}" == "xxksu" ]]; then

                KERNELSU_AUTHOR="backslashxx"
                KERNELSU_NAME="KernelSU"
                KERNELSU_ORIGIN_BRANCH="master"
                KERNELSU_BRANCH="master"


            elif [[ "${{ env.KERNELSU_AUTO_FORK }}" == "rsuntk" ]]; then

                KERNELSU_AUTHOR="rsuntk"
                KERNELSU_NAME="KernelSU"
                KERNELSU_ORIGIN_BRANCH="main"
                KERNELSU_BRANCH="main"


            elif [[ "${{ env.KERNELSU_AUTO_FORK }}" == "sukisu" ]]; then

                KERNELSU_AUTHOR="SukiSU-Ultra"
                KERNELSU_NAME="SukiSU-Ultra"
                KERNELSU_ORIGIN_BRANCH="main"
                KERNELSU_BRANCH="builtin"


            elif [[ "${{ env.KERNELSU_AUTO_FORK }}" == "next" ]]; then

                KERNELSU_AUTHOR="KernelSU-Next"
                KERNELSU_NAME="KernelSU-Next"
                KERNELSU_ORIGIN_BRANCH="legacy"
                KERNELSU_BRANCH="legacy"


            elif [[ "${{ env.KERNELSU_AUTO_FORK }}" == "resukisu" ]]; then

                KERNELSU_AUTHOR="ReSukiSU"
                KERNELSU_NAME="ReSukiSU"
                KERNELSU_ORIGIN_BRANCH="main"
                KERNELSU_BRANCH="main"


            else

                echo "[-] Please input valid fork name!"
                exit 1

            fi


            # --------------------------------------------------------
            # KernelSU-Next
            #
            # Always use:
            #
            # device_kernel/KernelSU
            #
            # even though the repository itself is named
            # KernelSU-Next.
            # --------------------------------------------------------

            if [[ "${{ env.KERNELSU_AUTO_FORK }}" == "next" ]]; then

                KERNELSU_FOLDER="KernelSU"

                echo "KERNELSU_NAME=KernelSU-Next" >> "$GITHUB_ENV"

                ksu_dir="$GITHUB_WORKSPACE/device_kernel/KernelSU"

                if [[ ! -d "$ksu_dir" ]]; then
                    echo "::error::KernelSU directory is missing: $ksu_dir"
                    exit 1
                fi

                if [[ ! -f "$ksu_dir/kernel/Makefile" ]]; then
                    echo "::error::KernelSU checkout is invalid: $ksu_dir"
                    echo "::error::Expected: $ksu_dir/kernel/Makefile"
                    exit 1
                fi

                echo "[+] KernelSU-Next detected."
                echo "[+] Repository: $KERNELSU_AUTHOR/$KERNELSU_NAME"
                echo "[+] Branch: $KERNELSU_BRANCH"
                echo "[+] Local path: $ksu_dir"


            else

                KERNELSU_FOLDER="KernelSU"

                echo "KERNELSU_NAME=$KERNELSU_FOLDER" >> "$GITHUB_ENV"

            fi


            # --------------------------------------------------------
            # KernelSU installation method
            # --------------------------------------------------------

            if [[ "${{ env.KERNELSU_METHOD }}" == "shell" ]]; then

                # ----------------------------------------------------
                # KernelSU-Next has already been cloned by the main
                # workflow.
                #
                # DO NOT execute setup.sh again.
                # ----------------------------------------------------

                if [[ "${{ env.KERNELSU_AUTO_FORK }}" == "next" ]]; then

                    ksu_dir="$GITHUB_WORKSPACE/device_kernel/KernelSU"

                    if [[ ! -d "$ksu_dir" ]]; then
                        echo "::error::KernelSU directory is missing: $ksu_dir"
                        exit 1
                    fi

                    if [[ ! -f "$ksu_dir/kernel/Makefile" ]]; then
                        echo "::error::KernelSU checkout is invalid: $ksu_dir"
                        exit 1
                    fi

                    echo "[+] Using pre-cloned KernelSU-Next."
                    echo "[+] Path: $ksu_dir"

                else

                    curl -sSL \
                      "https://raw.githubusercontent.com/${KERNELSU_AUTHOR}/${KERNELSU_NAME}/refs/heads/${KERNELSU_ORIGIN_BRANCH}/kernel/setup.sh" \
                      | bash -s "$KERNELSU_BRANCH"

                fi


            elif [[ "${{ env.KERNELSU_METHOD }}" == "manual" ]]; then

                git clone \
                  "https://github.com/$KERNELSU_AUTHOR/$KERNELSU_NAME.git" \
                  -b "$KERNELSU_BRANCH" \
                  "$KERNELSU_FOLDER"

                ln -sfn \
                  "../$KERNELSU_FOLDER/kernel" \
                  "drivers/kernelsu"

                grep -q "kernelsu" \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/Makefile" \
                  || printf "\nobj-\$(CONFIG_KSU) += kernelsu/\n" \
                    >> "$GITHUB_WORKSPACE/device_kernel/drivers/Makefile"

                grep -q 'source "drivers/kernelsu/Kconfig"' \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/Kconfig" \
                  || sed -i \
                    '/endmenu/i\source "drivers/kernelsu/Kconfig"' \
                    "$GITHUB_WORKSPACE/device_kernel/drivers/Kconfig"

                echo "[+] KernelSU Settings done."


            elif [[ "${{ env.KERNELSU_METHOD }}" == "only" ]]; then

                git clone \
                  "https://github.com/$KERNELSU_AUTHOR/$KERNELSU_NAME.git" \
                  -b "$KERNELSU_BRANCH" \
                  "$KERNELSU_FOLDER"

                ln -sfn \
                  "../$KERNELSU_FOLDER/kernel" \
                  "drivers/kernelsu"

            fi


            # --------------------------------------------------------
            # For the pre-cloned KernelSU-Next tree, explicitly
            # connect it to the kernel source.
            # --------------------------------------------------------

            if [[ "${{ env.KERNELSU_AUTO_FORK }}" == "next" ]]; then

                ksu_dir="$GITHUB_WORKSPACE/device_kernel/KernelSU"

                ln -sfn \
                  "../KernelSU/kernel" \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/kernelsu"

                grep -q "kernelsu" \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/Makefile" \
                  || printf "\nobj-\$(CONFIG_KSU) += kernelsu/\n" \
                    >> "$GITHUB_WORKSPACE/device_kernel/drivers/Makefile"

                grep -q 'source "drivers/kernelsu/Kconfig"' \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/Kconfig" \
                  || sed -i \
                    '/endmenu/i\source "drivers/kernelsu/Kconfig"' \
                    "$GITHUB_WORKSPACE/device_kernel/drivers/Kconfig"

                echo "[+] KernelSU-Next linked to kernel source."
                echo "[+] drivers/kernelsu -> ../KernelSU/kernel"

            fi

          }


          # ==========================================================
          # Manual KernelSU
          # ==========================================================

          KERNELSU_MANUAL(){

            if [[ "${{ env.KERNELSU_SOURCE }}" == *Next* ]]; then

                KERNELSU_FOLDER="KernelSU"

                echo "KERNELSU_NAME=KernelSU-Next" >> "$GITHUB_ENV"

            else

                KERNELSU_FOLDER="KernelSU"

                echo "KERNELSU_NAME=$KERNELSU_FOLDER" >> "$GITHUB_ENV"

            fi


            if [[ "${{ env.KERNELSU_METHOD }}" == "shell" ]]; then

                curl -sSL \
                  "${{ env.KERNELSU_SOURCE }}" \
                  | bash -s "${{ env.KERNELSU_BRANCH }}"


            elif [[ "${{ env.KERNELSU_METHOD }}" == "manual" ]]; then

                git clone \
                  "${{ env.KERNELSU_SOURCE }}" \
                  -b "${{ env.KERNELSU_BRANCH }}" \
                  "$KERNELSU_FOLDER"

                ln -sfn \
                  "../$KERNELSU_FOLDER/kernel" \
                  "drivers/kernelsu"

                grep -q "kernelsu" \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/Makefile" \
                  || printf "\nobj-\$(CONFIG_KSU) += kernelsu/\n" \
                    >> "$GITHUB_WORKSPACE/device_kernel/drivers/Makefile"

                grep -q 'source "drivers/kernelsu/Kconfig"' \
                  "$GITHUB_WORKSPACE/device_kernel/drivers/Kconfig" \
                  || sed -i \
                    '/endmenu/i\source "drivers/kernelsu/Kconfig"' \
                    "$GITHUB_WORKSPACE/device_kernel/drivers/Kconfig"

                echo "[+] KernelSU Settings done."


            elif [[ "${{ env.KERNELSU_METHOD }}" == "only" ]]; then

                git clone \
                  "${{ env.KERNELSU_SOURCE }}" \
                  -b "${{ env.KERNELSU_BRANCH }}" \
                  "$KERNELSU_FOLDER"

                ln -sfn \
                  "../$KERNELSU_FOLDER/kernel" \
                  "drivers/kernelsu"

            fi

          }


          # ==========================================================
          # Core
          # ==========================================================

          if [[ "${{ env.KERNELSU_AUTO_GET }}" == "true" ]]; then

            KERNELSU_AUTO

          else

            KERNELSU_MANUAL

          fi

          echo "KERNELSU_METHOD=${{ env.KERNELSU_METHOD }}" >> "$GITHUB_ENV"


      # ============================================================
      # SukiSU-Ultra Fixed
      # ============================================================

      - name: SukiSU-Ultra Fixed
        shell: bash
        run: |
          set -euxo pipefail

          ksu_link="$GITHUB_WORKSPACE/device_kernel/drivers/kernelsu"

          if [[ ! -e "$ksu_link" ]]; then
            echo "::error::drivers/kernelsu is missing: $ksu_link"
            exit 1
          fi

          cd "$ksu_link"

          if [[ "${{ env.KERNELSU_SOURCE }}" == *SukiSU* ]] || \
             [[ "${{ env.KERNELSU_AUTO_FORK }}" == "sukisu" ]]; then

            cp /tmp/Patches/Patch/sukisu_ultra_fixed.patch ./

            patch -p1 < sukisu_ultra_fixed.patch

            echo "[+] SukiSU-Ultra mitigation measures implemented."

          else

            echo "[-] Your KernelSU Fork needn't it, Skipped."

          fi


      # ============================================================
      # xxksu add SuSFS
      # ============================================================

      - name: xxksu add SuSFS
        shell: bash
        run: |
          set -euxo pipefail

          ksu_dir="$GITHUB_WORKSPACE/device_kernel/KernelSU"

          if [[ ! -d "$ksu_dir" ]]; then
            echo "::error::KernelSU directory is missing: $ksu_dir"
            exit 1
          fi

          cd "$ksu_dir"

          if [[ "${{ env.KERNELSU_SOURCE }}" == *backslashxx* ]] || \
             [[ "${{ env.KERNELSU_AUTO_FORK }}" == "xxksu" ]]; then

            if [[ "${{ env.SUSFS_ENABLE }}" == "true" ]]; then

              patch -p1 < <(
                curl -sL \
                  "https://github.com/midori01/KernelSU/commit/xx.patch"
              ) || true

              echo "[+] xxksu susfs applied."

            fi

          else

            echo "[-] Your KernelSU Fork needn't it, Skipped."

          fi


      # ============================================================
      # KPM Function Support
      # ============================================================

      - name: KPM Function Support
        if: env.KPM_ENABLE == 'true'
        shell: bash
        run: |
          set -euxo pipefail

          cd "$GITHUB_WORKSPACE/device_kernel"

          ENABLE_KPM_CONFIGS(){

            sed -n \
              '/^config KALLSYMS_ALL/,/^[^ \t]/p' \
              init/Kconfig |
              grep -q 'depends on.*DEBUG_KERNEL' &&
              echo "CONFIG_DEBUG_KERNEL=y" \
                >> "./arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

            echo "CONFIG_KALLSYMS=y" \
              >> "./arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

            echo "CONFIG_KALLSYMS_ALL=y" \
              >> "./arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

          }


          GET_KPM_TOOLS(){

            echo "[+] Detected set_memory."

            echo "CONFIG_KPM=y" \
              >> "./arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"

            ${{ env.CURLX }} \
              "${{ env.KPM_PATCH_SOURCE }}" \
              patch

            if [[ -f "patch" ]]; then

                chmod a+x patch

                echo "[+] Get KPM Tools successfully!"

            else

                echo "[-] Get KPM Tools failed!"

                exit 1

            fi

          }


          GET_KPATCH_NEXT_TOOLS(){

            REPO="KernelSU-Next/KPatch-Next"

            ${{ env.CURLX }} \
              "https://github.com/$REPO/releases/latest/download/kpimg-linux" \
              kpimg-linux

            ${{ env.CURLX }} \
              "https://github.com/$REPO/releases/latest/download/kptools-linux" \
              kptools-linux

            if [[ -f "kptools-linux" ]]; then

                chmod a+x kptools-linux

                echo "[+] Get KPatch-Next Tools successfully!"

            else

                echo "[-] Get KPatch-Next Tools failed!"

                exit 1

            fi

          }


          if [[ "${{ env.KERNELSU_SOURCE }}" == *SukiSU* ]] || \
             [[ "${{ env.KERNELSU_AUTO_FORK }}" == "sukisu" ]] && \
             [[ "${{ env.KPM_INLINE }}" == "true" ]]; then

            echo "[+] Detected SukiSU-Ultra and enabled KPM Inline."

            ENABLE_KPM_CONFIGS

            if [[ -f "include/linux/set_memory.h" ]]; then

                GET_KPM_TOOLS

            else

                cp /tmp/Patches/Patch/set_memory_to_49_and_low.patch ./

                patch -p1 < set_memory_to_49_and_low.patch || true

                echo "[+] Patch set_memory successfully!"
                echo "[-] If have patch error (.rej), manual fixed it."

                GET_KPM_TOOLS

            fi


          elif [[ "${{ env.KERNELSU_SOURCE }}" != *SukiSU* ]] || \
               [[ "${{ env.KERNELSU_AUTO_FORK }}" != "sukisu" ]] && \
               [[ "${{ env.KPM_INLINE }}" == "true" ]]; then

            echo "[-] Your KernelSU Fork could not support KPM Inline!"

            exit 1


          elif [[ "${{ env.KPM_KPATCH_NEXT }}" == "true" ]]; then

            GET_KPATCH_NEXT_TOOLS


          else

            ENABLE_KPM_CONFIGS

            echo "[+] Necessary configuration items have been enabled for the KPM feature."

          fi


      # ============================================================
      # Set KSU for DEFCONFIG
      # ============================================================

      - name: Set KSU for DEFCONFIG
        shell: bash
        run: |
          set -euxo pipefail

          cd "$GITHUB_WORKSPACE/device_kernel"

          echo "CONFIG_KSU=y" \
            >> "./arch/${{ env.KERNEL_ARCH }}/configs/${{ env.DEFCONFIG_NAME }}"